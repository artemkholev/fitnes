# Быстрый старт

## За 5 минут до запуска

### 1. Получите токен бота
Откройте [@BotFather](https://t.me/BotFather) в Telegram:
```
/newbot
Название бота: Мой Фитнес Бот
Username: my_fitness_bot
```
Скопируйте токен (например: `1234567890:ABCdefGHI...`)

### 2. Настройте .env
```bash
cp .env.example .env
nano .env
```

Вставьте ваш токен:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

### 3. Запустите
```bash
docker-compose up -d
```

### 4. Проверьте
```bash
docker-compose logs -f bot
```

Готово! Найдите бота в Telegram и отправьте `/start`

---

## Автоматическая установка

Используйте скрипт установки:
```bash
chmod +x setup.sh
./setup.sh
```

---

## Полезные команды

```bash
# Просмотр логов
make logs

# Остановка
make down

# Перезапуск
make restart

# Backup БД
make backup

# Помощь
make help
```

---

## Что дальше?

- **[README.md](README.md)** - Полная документация
- **[DEPLOY.md](DEPLOY.md)** - Детальная инструкция по деплою
- **[EXAMPLES.md](EXAMPLES.md)** - Примеры использования

---

## Минимальные требования

- Docker 20.10+
- Docker Compose 1.29+
- 1 GB RAM
- 5 GB дискового пространства

---

## Структура проекта

```
fitnes/
├── cmd/bot/main.go              # Точка входа
├── internal/
│   ├── bot/                     # Логика бота
│   ├── database/                # БД операции
│   ├── handlers/                # Обработчики команд
│   ├── models/                  # Модели данных
│   └── charts/                  # Графики
├── migrations/001_init.sql      # Схема БД
├── docker-compose.yml           # Конфигурация Docker
├── Dockerfile                   # Образ приложения
├── Makefile                     # Команды управления
└── README.md                    # Документация
```

---

## Основные возможности

### Для клиентов:
- ➕ Добавление тренировок
- 📸 Фото упражнений
- 📊 Графики прогресса
- 🔍 Поиск тренера
- 📅 Групповые тренировки

### Для тренеров:
- 👥 Управление клиентами
- 📅 Создание групповых занятий
- 📊 Мониторинг прогресса

---

## Устранение проблем

**Бот не запускается:**
```bash
docker-compose logs bot
# Проверьте токен в .env
```

**База данных не подключается:**
```bash
docker-compose restart postgres
docker-compose logs postgres
```

**Порт занят:**
```bash
sudo lsof -i :5432
# Остановите конфликтующий процесс
```

---

## Поддержка

Проблемы? Проверьте:
1. Логи: `docker-compose logs -f`
2. Статус: `docker-compose ps`
3. .env файл корректен
4. Docker работает: `docker ps`

Подробнее в [README.md](README.md)
