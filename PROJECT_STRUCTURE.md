# Структура проекта

```
fitnes/
│
├── 📄 Документация
│   ├── README.md              # Основная документация
│   ├── QUICKSTART.md          # Быстрый старт
│   ├── DEPLOY.md              # Детальная инструкция по деплою
│   ├── EXAMPLES.md            # Примеры использования
│   └── PROJECT_STRUCTURE.md   # Этот файл
│
├── 🐳 Docker конфигурация
│   ├── Dockerfile             # Сборка Docker образа
│   ├── docker-compose.yml     # Orchestration (БД + Бот)
│   └── .env.example           # Пример переменных окружения
│
├── 🛠 Утилиты
│   ├── Makefile               # Команды для управления проектом
│   ├── setup.sh               # Автоматическая установка
│   └── .gitignore             # Git игнорирование
│
├── 📁 cmd/
│   └── bot/
│       └── main.go            # Точка входа приложения
│
├── 📁 internal/               # Внутренний код (не импортируется извне)
│   │
│   ├── models/                # Модели данных
│   │   └── models.go          # User, Workout, Exercise, GroupTraining
│   │
│   ├── database/              # Слой работы с PostgreSQL
│   │   ├── database.go        # Подключение к БД
│   │   ├── users.go           # CRUD операции для пользователей
│   │   ├── workouts.go        # CRUD операции для тренировок
│   │   ├── organizations.go   # CRUD операции для организаций
│   │   └── group_trainings.go # CRUD операции для групповых тренировок
│   │
│   ├── bot/                   # Логика Telegram бота
│   │   ├── bot.go             # Основная структура бота
│   │   └── keyboards.go       # Клавиатуры и меню
│   │
│   ├── handlers/              # Обработчики команд и сообщений
│   │   ├── start.go           # Регистрация пользователей
│   │   ├── workout.go         # Добавление и просмотр тренировок
│   │   ├── trainer.go         # Функции тренера
│   │   ├── group_training.go  # Групповые тренировки
│   │   └── stats.go           # Статистика и графики
│   │
│   └── charts/                # Генерация графиков
│       └── charts.go          # Графики прогресса
│
├── 📁 migrations/             # SQL миграции
│   └── 001_init.sql           # Начальная схема БД
│
└── 📁 backups/                # Backup базы данных (создается автоматически)
```

## Описание модулей

### cmd/bot/main.go
**Назначение:** Точка входа в приложение
**Функции:**
- Загрузка переменных окружения
- Подключение к базе данных
- Инициализация Telegram бота
- Основной цикл обработки обновлений

### internal/models/models.go
**Назначение:** Определение структур данных
**Модели:**
- `User` - Пользователь (клиент/тренер)
- `Organization` - Организация
- `Workout` - Тренировка
- `Exercise` - Упражнение
- `GroupTraining` - Групповая тренировка
- `GroupTrainingParticipant` - Участник групповой тренировки
- `UserState` - Состояние пользователя в диалоге

### internal/database/
**Назначение:** Работа с PostgreSQL
**Файлы:**
- `database.go` - Подключение к БД
- `users.go` - Операции с пользователями
- `workouts.go` - Операции с тренировками
- `organizations.go` - Операции с организациями
- `group_trainings.go` - Операции с групповыми тренировками

### internal/bot/
**Назначение:** Core логика бота
**Файлы:**
- `bot.go` - Структура бота, управление состояниями
- `keyboards.go` - Telegram клавиатуры (меню)

### internal/handlers/
**Назначение:** Обработчики пользовательских действий
**Файлы:**
- `start.go` - /start, регистрация
- `workout.go` - Добавление/просмотр тренировок
- `trainer.go` - Поиск тренера, список клиентов
- `group_training.go` - Создание/запись на групповые тренировки
- `stats.go` - Статистика и графики

### internal/charts/
**Назначение:** Генерация визуализаций
**Файлы:**
- `charts.go` - Создание графиков прогресса

### migrations/
**Назначение:** SQL миграции
**Файлы:**
- `001_init.sql` - Создание таблиц и индексов

## Схема базы данных

```
organizations
├── id (PK)
├── name
├── code (UNIQUE)
└── created_at

users
├── id (PK)
├── telegram_id (UNIQUE)
├── username
├── full_name
├── role (client/trainer)
├── organization_id (FK → organizations)
├── trainer_id (FK → users)
└── created_at

workouts
├── id (PK)
├── user_id (FK → users)
├── trainer_id (FK → users)
├── date
├── notes
├── muscle_group
└── created_at

exercises
├── id (PK)
├── workout_id (FK → workouts)
├── name
├── sets
├── reps
├── weight
├── rest_seconds
├── photo_file_id
├── notes
├── order
└── created_at

group_trainings
├── id (PK)
├── organization_id (FK → organizations)
├── trainer_id (FK → users)
├── name
├── description
├── scheduled_at
├── max_participants
└── created_at

group_training_participants
├── id (PK)
├── group_training_id (FK → group_trainings)
├── user_id (FK → users)
└── joined_at
```

## Потоки данных

### Добавление тренировки
```
Пользователь → Telegram API
     ↓
handlers/workout.go::HandleAddWorkout()
     ↓
bot/bot.go::SetState("awaiting_muscle_group")
     ↓
handlers/workout.go::HandleMuscleGroupSelection()
     ↓
database/workouts.go::CreateWorkout()
     ↓
PostgreSQL
```

### Просмотр статистики
```
Пользователь → Telegram API
     ↓
handlers/stats.go::HandleStats()
     ↓
database/workouts.go::GetExerciseStats()
     ↓
charts/charts.go::GenerateProgressChart()
     ↓
Telegram API (отправка графика)
```

## Зависимости

### Go модули
```
github.com/go-telegram-bot-api/telegram-bot-api/v5  # Telegram Bot API
github.com/jackc/pgx/v5                             # PostgreSQL драйвер
github.com/joho/godotenv                            # Загрузка .env
github.com/wcharczuk/go-chart/v2                    # Генерация графиков
```

### Docker образы
```
golang:1.21-alpine    # Сборка приложения
alpine:latest         # Runtime
postgres:15-alpine    # База данных
```

## Переменные окружения

```env
TELEGRAM_BOT_TOKEN    # Токен бота от @BotFather
DB_HOST               # Хост PostgreSQL (postgres в Docker)
DB_PORT               # Порт PostgreSQL (5432)
DB_USER               # Пользователь БД
DB_PASSWORD           # Пароль БД
DB_NAME               # Имя базы данных
APP_ENV               # Окружение (production/development)
```

## Команды Makefile

```bash
make help      # Показать помощь
make build     # Собрать Docker образ
make up        # Запустить проект
make down      # Остановить проект
make restart   # Перезапустить
make logs      # Показать все логи
make logs-bot  # Логи бота
make logs-db   # Логи БД
make status    # Статус контейнеров
make backup    # Backup БД
make restore   # Восстановить БД
make db-shell  # Shell PostgreSQL
make clean     # Удалить все (с данными!)
```

## Процесс разработки

### Локальная разработка
```bash
# 1. Установить зависимости
make install-deps

# 2. Запустить БД в Docker
docker-compose up -d postgres

# 3. Создать .env для локального запуска
cp .env.example .env
# Изменить DB_HOST=localhost

# 4. Запустить бота локально
make run-local
```

### Деплой на сервер
```bash
# 1. Скопировать файлы на сервер
scp -r . user@server:/home/user/fitness-bot

# 2. На сервере
cd /home/user/fitness-bot
./setup.sh

# 3. Или вручную
docker-compose up -d
```

## Мониторинг

### Логи
```bash
docker-compose logs -f bot       # Логи бота
docker-compose logs -f postgres  # Логи БД
```

### Статус
```bash
docker-compose ps                # Статус контейнеров
docker stats                     # Использование ресурсов
```

### База данных
```bash
# Войти в БД
make db-shell

# Просмотр таблиц
\dt

# Количество пользователей
SELECT COUNT(*) FROM users;

# Статистика тренировок
SELECT u.full_name, COUNT(w.id) as workouts
FROM users u
LEFT JOIN workouts w ON u.id = w.user_id
GROUP BY u.id, u.full_name;
```

## Расширение функционала

### Добавление новой команды
1. Создать handler в `internal/handlers/`
2. Добавить обработку в `cmd/bot/main.go::handleUpdate()`
3. Опционально добавить кнопку в `internal/bot/keyboards.go`

### Добавление новой таблицы
1. Создать миграцию в `migrations/`
2. Добавить модель в `internal/models/models.go`
3. Создать CRUD операции в `internal/database/`

### Пример: Добавление "Заметок тренера"

**1. Миграция (migrations/002_trainer_notes.sql)**
```sql
CREATE TABLE trainer_notes (
    id SERIAL PRIMARY KEY,
    trainer_id INTEGER REFERENCES users(id),
    client_id INTEGER REFERENCES users(id),
    note TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**2. Модель (internal/models/models.go)**
```go
type TrainerNote struct {
    ID        int64
    TrainerID int64
    ClientID  int64
    Note      string
    CreatedAt time.Time
}
```

**3. БД операции (internal/database/trainer_notes.go)**
```go
func (db *DB) CreateTrainerNote(ctx context.Context, note *TrainerNote) error {
    // ...
}
```

**4. Handler (internal/handlers/trainer_notes.go)**
```go
func HandleAddNote(b *bot.Bot, message *tgbotapi.Message) {
    // ...
}
```

## Безопасность

### Рекомендации
- ✅ Никогда не коммитить .env
- ✅ Использовать сложные пароли БД
- ✅ Регулярно обновлять Docker образы
- ✅ Настроить firewall на сервере
- ✅ Делать регулярные backup БД
- ✅ Ограничить доступ к PostgreSQL порту

### Backup стратегия
```bash
# Ежедневный автоматический backup
0 3 * * * /home/user/fitness-bot/backup.sh

# Хранение backup 30 дней
find ~/backups -name "*.sql" -mtime +30 -delete
```

## Производительность

### Оптимизации
- Индексы на часто запрашиваемых полях
- Connection pooling в pgx
- Кэширование состояний пользователей в памяти
- Асинхронная обработка сообщений через goroutines

### Масштабирование
Для большой нагрузки:
- Использовать Redis для хранения состояний
- Добавить горизонтальное масштабирование бота
- Настроить репликацию PostgreSQL
- Использовать CDN для статических файлов (графики)

---

Проект готов к деплою и использованию!
