# Фитнес Бот для Telegram

Телеграм-бот для отслеживания тренировок, связи тренеров и клиентов, групповых тренировок и анализа прогресса.

## Возможности

### Для клиентов:
- ➕ Добавление тренировок с упражнениями
- 📸 Прикрепление фото к упражнениям
- 📝 Просмотр истории тренировок
- 🔍 Поиск и привязка к тренеру
- 📅 Запись на групповые тренировки
- 📊 Статистика и графики прогресса

### Для тренеров:
- ➕ Создание тренировок для клиентов
- 👥 Просмотр списка клиентов
- 📅 Создание групповых тренировок
- 📊 Просмотр статистики клиентов

## Технологии

- **Backend**: Go 1.21+
- **Database**: PostgreSQL 15
- **Telegram Bot API**: tgbotapi/v5
- **Charts**: go-chart/v2
- **Deployment**: Docker + Docker Compose

## Структура проекта

```
fitnes/
├── cmd/bot/           # Главный файл приложения
├── internal/
│   ├── bot/           # Логика бота
│   ├── database/      # Слой работы с БД
│   ├── handlers/      # Обработчики команд
│   ├── models/        # Модели данных
│   └── charts/        # Генерация графиков
├── migrations/        # SQL миграции
├── Dockerfile
├── docker-compose.yml
└── README.md
```

## Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Локальный запуск

1. **Клонируйте репозиторий или скопируйте файлы на сервер**

2. **Создайте .env файл**
```bash
cp .env.example .env
```

3. **Отредактируйте .env файл**
```bash
nano .env
```

Заполните переменные:
```env
# Telegram Bot Token от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=fitness_user
DB_PASSWORD=your_secure_password
DB_NAME=fitness_bot

# Application
APP_ENV=production
```

4. **Запустите проект**
```bash
docker-compose up -d
```

5. **Проверьте логи**
```bash
docker-compose logs -f bot
```

6. **Остановка проекта**
```bash
docker-compose down
```

### Деплой на сервер

#### Подготовка сервера

Убедитесь, что на сервере установлены:
- Docker
- Docker Compose
- Git (опционально)

Если нет, установите:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
```

Выйдите и войдите снова для применения изменений.

#### Деплой проекта

1. **Скопируйте проект на сервер**

Вариант 1: Через Git (если используете)
```bash
git clone your_repository_url
cd fitnes
```

Вариант 2: Через SCP
```bash
# На локальной машине
scp -r /path/to/fitnes user@your-server-ip:/home/user/
```

2. **Создайте .env файл на сервере**
```bash
cd fitnes
nano .env
```

Заполните данными (особенно TELEGRAM_BOT_TOKEN).

3. **Запустите проект**
```bash
docker-compose up -d
```

4. **Проверьте статус**
```bash
docker-compose ps
docker-compose logs -f bot
```

#### Настройка автозапуска

Создайте systemd сервис для автозапуска:

```bash
sudo nano /etc/systemd/system/fitness-bot.service
```

Содержимое файла:
```ini
[Unit]
Description=Fitness Telegram Bot
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/user/fitnes
ExecStart=/usr/local/bin/docker-compose up -d
ExecStop=/usr/local/bin/docker-compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable fitness-bot
sudo systemctl start fitness-bot
sudo systemctl status fitness-bot
```

## Использование бота

### Первый запуск

1. Найдите бота в Telegram по username
2. Отправьте `/start`
3. Выберите роль (Клиент или Тренер)
4. Введите код организации (опционально)

### Создание организации

Для создания организации выполните SQL запрос в базе данных:

```bash
# Войдите в контейнер с PostgreSQL
docker exec -it fitness_postgres psql -U fitness_user -d fitness_bot

# Создайте организацию
INSERT INTO organizations (name, code) VALUES ('Название организации', 'ORG2024');
```

### Основные команды

- `/start` - Начать работу / Показать главное меню
- `/creategroup` - Создать групповую тренировку (только для тренеров)

### Добавление тренировки

1. Нажмите "➕ Добавить тренировку"
2. Выберите группу мышц
3. Введите упражнение в формате:
```
Название
Подходы
Повторения
Вес (кг)
```

Пример:
```
Жим лежа
4
10
80
```

4. Опционально отправьте фото
5. Добавьте ещё упражнения или нажмите "✅ Завершить"

## Управление проектом

### Обновление кода

```bash
# Остановите контейнеры
docker-compose down

# Обновите код (git pull или загрузите новые файлы)
git pull

# Пересоберите и запустите
docker-compose up -d --build
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только бот
docker-compose logs -f bot

# Только база данных
docker-compose logs -f postgres
```

### Резервное копирование базы данных

```bash
# Создание backup
docker exec fitness_postgres pg_dump -U fitness_user fitness_bot > backup_$(date +%Y%m%d).sql

# Восстановление из backup
docker exec -i fitness_postgres psql -U fitness_user fitness_bot < backup_20260118.sql
```

### Очистка и перезапуск

```bash
# Полная очистка (ВНИМАНИЕ: удалит все данные!)
docker-compose down -v

# Запуск заново
docker-compose up -d
```

## Устранение проблем

### Бот не отвечает

1. Проверьте логи: `docker-compose logs -f bot`
2. Проверьте токен бота в .env
3. Убедитесь, что бот не запущен где-то ещё (Telegram позволяет только 1 активное подключение)

### Ошибка подключения к БД

1. Проверьте статус: `docker-compose ps`
2. Проверьте логи БД: `docker-compose logs -f postgres`
3. Убедитесь, что порт 5432 свободен

### Не генерируются графики

1. Проверьте, что есть данные для графика
2. Проверьте логи на ошибки: `docker-compose logs -f bot | grep chart`

## Безопасность

- Никогда не коммитьте .env файл в Git
- Используйте сложные пароли для базы данных
- Регулярно обновляйте Docker образы
- Настройте firewall на сервере

## Лицензия

MIT

## Поддержка

При возникновении проблем создайте issue в репозитории проекта.
